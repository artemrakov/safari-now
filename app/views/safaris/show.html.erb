<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
     <% @safari.safari_images.each_with_index do |photo, n| %>
       <li data-target='#MyCarousel' data-slide-to="#{n}" class="#{'active' if n == 0}"></li>
     <% end %>
  </ol>

  <div class="carousel-inner" role="listbox">
    <div class="item active show-card">
      <div class="safari-image" style="background-image: url('<%= cl_image_path @safari.safari_images.first.photo, height: 500, width: 2000, crop: :fill %>'); height: 500px; width: 100vw; background-position: center; background-size: cover"></div>
    </div>
    <% @safari.safari_images.drop(1).each do |photo| %>
      <div class="item show-card">
        <div class="safari-image" style="background-image: url('<%= cl_image_path photo.photo, height: 500, width: 2000 %>'); height: 500px; width: 100vw; background-position: center; background-size: cover"></div>
      </div>
    <% end %>
  </div>

  <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<!-- Left and right controls -->

  <!-- <div class="show-banner-content">
    <h2><%# @safari.title %></h2>
    <%# cl_image_tag @user.avatar_url, class: "safari-owner" %>

  <a class="safari-owner-link" href="#" ></a>
  </div> -->
<div class="container">

<div class="info">
  <div class="summary">
    <p>£<%= @safari.price %></p>
  </div>
  <div class="book-button">
    <a href="#">Book now</a>
  </div>

  <h3><%= @safari.address %> - <%= @safari.date %></h3>
  <p><%= @safari.description %></p>
</div>
</div>


<div id="map" style="width: 100%; height: 400px;"></div>

<div class="reviews">

</div>
<% if current_user %>
  <%= link_to "Book", create_booking_path(@safari, current_user), method: :post %>
<% else %>
  <%= link_to "Book", new_user_session_path %>
<% end %>
<%= content_for(:before_js) do %>
  <%= javascript_include_tag "https://maps.google.com/maps/api/js?key=AIzaSyCMtlyoljyulu4QGTB6SxcjUMxW-lCmv2I" %>
  <%= javascript_include_tag "https://cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/markerclusterer/src/markerclusterer_compiled.js" %>
  <%= javascript_include_tag "https://cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js" %>
<% end %>

<% content_for(:after_js) do %>

  <script>
    $(document).ready(function() {
      var handler = Gmaps.build('Google');
      handler.buildMap({ internal: { id: 'map' } }, function() {
        markers = handler.addMarkers([<%= raw @safari_coordinates.to_json %>]);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        if (markers.length == 0) {
          handler.getMap().setZoom(2);
        } else if (markers.length == 1) {
          handler.getMap().setZoom(6);
        }
      });
    });
  </script>


<% end %>





