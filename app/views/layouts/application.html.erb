<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <link href="https://fonts.googleapis.com/css?family=Julius+Sans+One|Muli:300,400,600|Source+Sans+Pro:300,400,600" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1">
    <title><%= yield(:title_tag) || "Safari" %></title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= favicon_link_tag %>
  </head>
  <body>

    <%= render partial: 'layouts/navbar' %>
    <%= render "shared/flashes" %>
    <div class="wrapper">
    <% if devise_controller? %>
      <div class="container">
        <div class="row">
          <div class="col-xs-12 col-md-6 col-md-offset-3 devise-form">
            <%= yield %>
          </div>
        </div>
      </div>
    <% else %>
      <%= yield %>
    <% end %>
    </div>
    <%= yield(:before_js) %>
    <%= javascript_include_tag "application" %>
    <%= yield(:after_js) %>
  </body>
</html>
